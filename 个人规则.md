# 个人编码规则

## AI 编码风格偏好

### 代码可读性优先
- 优先选择清晰、易读的实现方式
- 避免过度优化和"一行代码"写法
- 使用有意义的变量和函数命名

### 注释风格
- 注释简洁明了，解释"为什么"而不是"是什么"
- 公共 API 使用文档注释 (///)
- 复杂逻辑添加行内注释说明

### 实现方式选择
- 优先使用标准库功能
- 遵循 Rust 惯用模式和最佳实践
- 保持代码简洁但不过度压缩

### 代码组织
- 函数保持单一职责
- 合理使用模块划分
- 避免过深的嵌套层级

## 工具链配置
- 使用 `cargo fmt` 自动格式化代码
- 使用 `cargo clippy` 检查代码质量
- 项目使用 Rust 2024 Edition

### rustfmt 配置
项目使用 `rustfmt.toml` 配置文件，采用默认格式化方式：

#### 基础格式化设置
- `max_width = 100`：每行最大宽度 100 字符
- `hard_tabs = false`：使用空格而非制表符
- `tab_spaces = 4`：每个缩进使用 4 个空格
- `newline_style = "Unix"`：使用 LF 换行符
- `use_small_heuristics = "Default"`：使用默认启发式设置

#### 代码组织
- `reorder_imports = true`：自动重新排序 imports 语句
- `reorder_modules = true`：自动重新排序模块声明
- `use_field_init_shorthand = true`：使用字段初始化简写
- `use_try_shorthand = false`：不使用 try! 宏简写

## 依赖库版本
- **rand**: 使用 `0.10` 版本
  - 创建 RNG: `let mut rng = rand::rng();`
  - 生成随机数: `rng.random_range(1..=100)` 或 `rng.random::<u32>()`
  - 导入: `use rand::prelude::*;`
